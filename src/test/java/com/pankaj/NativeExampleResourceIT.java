package com.pankaj;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeExampleResourceIT extends AsyncResourceTest {

    // Execute the same tests but in native mode.
}